/**
 * العربية (Arabic). Empty stub — falls back to English.
 *
 * Note: Arabic is RTL — it's listed in `RTL_LOCALES` so the i18n bootstrap
 * sets `<html dir="rtl">` automatically when this locale is active.
 */
export default {}
