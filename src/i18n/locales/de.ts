/**
 * Deutsch (German). Empty stub — every key falls back to English until filled.
 *
 * Use `src/i18n/locales/en.ts` as the schema. You only need to declare the
 * keys you want to translate; missing ones inherit `en` automatically.
 */
export default {}
